{
  "name": "acadeconnect",
  "version": "1.0.0",
  "private": true,
  "description": "AcadeConnect - Plataforma completa de gestão de eventos acadêmicos (React + Express + Prisma + PostgreSQL)",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start:backend": "npm --prefix backend run start",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "prisma:migrate": "npm --prefix backend run prisma:migrate",
    "prisma:migrate:deploy": "npm --prefix backend run prisma:migrate:deploy",
    "prisma:seed": "npm --prefix backend run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
